/* CSS is how you can add style to your website, such as colors, fonts, and positioning of your
  HTML content. To learn how to do something, just try searching Google for questions like
   "how to change link color." */


body, div, main, section, article {
  box-sizing: border-box; 
}

/* universal background color */
body {
  background-repeat: repeat-y;
  background-size: contain;
  width: 100%;
  background-position: top center; 
  background-attachment: scroll;
  background-color: #000000;
}



/* header image */
header img {
  width: 1000px;
  max-width: 100%;}
  
header img {
  background-color:#12A1FF;
  width: 100%;
}



/* clearfix hack to prevent image overflow. check out the W3Schools page on it. */
.clearfix::after {
  content: "";
  clear: both;
  display: table;
}

.img {
  width: 100%
  }
/*FONTS*/

/* header font */
#showComic, header, h1, h2, h3, h4, h5 {
  font-family: "Lacquer Regular"; color: #ffffff;
}

h1 {
  color: #EE4757;
  }

h2 {
  color: #EE4757;
  }


/* body font */
.subPage p, footer, #authorNotes, .archiveTable {
  font-family: 'Open Sans', sans-serif;
  font-size: large; color: #ffffff;

}

/* STYLING FOR SUBPAGES (about, characters, etc) */

/*general*/

.subPage {
  width: 850px;
  max-width: 90%;
  background-color: #000000;
  opacity: .7;
  outline: 5px solid #12A1FF;
  margin: auto;
  margin-bottom: 10px;
  padding: 0px 12px 12px;
}

.subPage:not(.archivePage) {
  text-align: center;
  
}

/* for pictures displayed to the left */
.leftPic {
  float:right;
  margin-left:20px;
}

/* for pictures displayed to the left */
.rightPic {

  float:left;
  margin-left:20px;
}


/* specific to Characters */
.charTable, .charTable td { 
  width: 100%;
}

.aboutTable, .aboutTable td {
  width: 100%;
  }

/* link colors */
a {
      color: #fff;
    }

a:hover {
      color: #27FFFF;
    }

/* HEADER */
header #nav {
  background-color: #12A1FF;
  outline: 4px solid #12A1FF;
  font-size: 25px;
  width: 100%;
  margin: auto;
}

/* HOMEPa

/* style nav button images */
.comicNav {
  display: flex;
  flex-wrap: nowrap;
  align-items: center;
  justify-content: center;
  padding: 10px 0px;
}
.comicNav img {
  width: 80px;
  max-width: 98%;
  padding-right: 30px;
}

/* style comic page image */
.comicPage img {
  width: 900px;
  max-width: 98%;
  outline: 3px solid #000000;
}

/* style author notes */
#authorNotes {
  background-color:#000000;
  outline: 3px solid #12A1FF;
  margin: auto;
  padding: 3px;
  padding-top: 0px;
  width: 900px;
  max-width: 98%;
}

#disqus_thread {
  max-width:98%;
  width: 900px;
  margin:auto;
  outline: 3px solid #12A1FF;
  background-color: #000000;
  padding: 10px;
  }
/* ARCHIVE PAGE */

/* style table in which archive is displayed */
.archiveTable {
  width: 90%;
  border-collapse:collapse;
}

/* style archive table cells */
.archiveTable td {
  padding: 3px;
  vertical-align: central;
  
}

/* style table cell in which page title is displayed */
.archiveCellTitle:not(.leftAlignTableText) {
  max-width: 300px;
  text-align: center;
}

.archiveCellDate {
  text-align: right;
  min-width: 120px;
}

.archiveCellNum {
  text-align: center;
  min-width: 30px;
}

/* style the thumbnails on the archive page */
.archiveCellThumb {
    width: 500px;
    max-width: 60px;
}
.archiveCellThumb img{
    max-width: 75%;
  }

/* for left aligning the text in a table cell */
.leftAlignTableText td {
  text-align: left;
}

/* highlight a table row and make pointer into hand when moused over */
.archiveRow:hover {
  background-color: #2ED1FF;
  cursor: pointer;
}

#myBtn {
  display: none; /* Hidden by default */
  position: fixed; /* Fixed/sticky position */
  bottom: 20px; /* Place the button at the bottom of the page */
  right: 30px; /* Place the button 30px from the right */
  z-index: 99; /* Make sure it does not overlap */
  border: none; /* Remove borders */
  outline: none; /* Remove outline */
  background-color: #12A1FF; /* Set a background color */
  color: white; /* Text color */
  cursor: pointer; /* Add a mouse pointer on hover */
  padding: 15px; /* Some padding */
  border-radius: 10px; /* Rounded corners */
  font-size: 18px; /* Increase font size */
}

#myBtn:hover {
  background-color: #555; /* Add a dark-grey background on hover */
}




/* FOOTER */
footer {

  color: #EE4757;
  margin-top: 12px;
  margin-bottom: 15px;
  float: left;
  width: 100%;
  font-size: 12px;
}

footer p {
  margin: auto;
}

footer a {
  color: #EE4757
}

footer a:hover {
  color: #E3E1E8
}

/* take away margins from the edges of the screen */
html, body {
  margin: 0;
}

/*message from GrapeApe: Below are some of my lame attempts to try and fix the problem on my phone*/

  @media only {  
  .SubPage img {
    max-width: 20px;
    } 
  .SubPage {
    max-width: 100%;}
    
  .iframe {
    width: 50%;}  
    
    }
    
  






